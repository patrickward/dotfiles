# ---------------------------------------------------------------------------
# Networking
# ---------------------------------------------------------------------------
# zref: @utils: Find my current IP using checkip.dyndns.org
alias whats-my-ip="curl -s checkip.dyndns.org | grep -Eo '[0-9\.]+'"

