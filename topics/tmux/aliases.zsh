# topics/tmux/aliases.zsh

alias tclear='tmux-clear'         # zref: Clear terminal + tmux scrollback history (see bin/tmux-clear)
alias tls='tmux list-sessions'    # zref: List all session
# alias tat='tmux attach -t'        # Superceded by bin/tat
